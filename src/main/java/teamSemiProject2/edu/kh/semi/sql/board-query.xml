<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<!-- 로그인 (탈퇴 회원 제외) -->
	<entry key="amountDown">
		UPDATE p_order SET product_amount = product_amount-1
		WHERE order_no = ?
	</entry>
	<entry key="getAddr">
		SELECT * FROM p_address
		WHERE member_no = ?
	</entry>
	<entry key="getOrderItem1">
		select o.order_no,p.product_no,p.product_nm,p.product_price,p.product_discount,o.product_amount
		from p_order o
		join p_product p
		on(o.product_no = p.product_no)
        where o.order_no = ?
	</entry>
	<entry key="getOrderItem2">
		OR o.order_no = ?
	</entry>




</properties>
